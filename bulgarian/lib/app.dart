import 'package:flutter/material.dart';
import 'bulgarian.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Bulgarian());
  }
}
